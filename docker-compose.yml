services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    env_file: .env

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    environment:
      - VITE_API_PROXY_TARGET=http://backend:8080
    depends_on:
      - backend
